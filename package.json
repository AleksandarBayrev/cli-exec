{
  "name": "cli-exec",
  "version": "1.0.0",
  "description": "Execute CLI commands from a list",
  "main": "build/app.js",
  "scripts": {
    "start": "npm run build && node ./build/index.js",
    "build": "tsc --build tsconfig.json",
    "test": "jest",
    "clean-build": "rm -rfv ./build"
  },
  "author": "Aleksandar Bayrev",
  "license": "GPL-3.0",
  "dependencies": {
    "@types/jest": "^26.0.23",
    "exec-sh": "^0.4.0",
    "jest-cli": "^27.0.6",
    "ts-jest": "^27.0.3",
    "typescript": "^4.3.5"
  }
}
